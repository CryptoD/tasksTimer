{
  "name": "tasktimer",
  "private": true,
  "version": "1.1.0",
  "description": "Dev tooling (ESLint, CI cache). The app is GJS/GTK; there are no npm runtime dependencies.",
  "scripts": {
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
